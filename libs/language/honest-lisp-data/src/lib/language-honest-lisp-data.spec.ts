import { languageHonestLispData } from './language-honest-lisp-data';

describe('languageHonestLispData', () => {
  it('should work', () => {
    expect(languageHonestLispData()).toEqual('language-honest-lisp-data');
  });
});
