import { languageHonestLispStdlib } from './language-honest-lisp-stdlib';

describe('languageHonestLispStdlib', () => {
  it('should work', () => {
    expect(languageHonestLispStdlib()).toEqual('language-honest-lisp-stdlib');
  });
});
