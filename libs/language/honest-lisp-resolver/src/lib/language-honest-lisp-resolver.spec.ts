import { languageHonestLispResolver } from './language-honest-lisp-resolver';

describe('languageHonestLispResolver', () => {
  it('should work', () => {
    expect(languageHonestLispResolver()).toEqual(
      'language-honest-lisp-resolver'
    );
  });
});
