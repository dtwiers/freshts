{
  "root": "libs/compose",
  "sourceRoot": "libs/compose/src",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/compose",
        "main": "libs/compose/src/index.ts",
        "tsConfig": "libs/compose/tsconfig.lib.json",
        "assets": ["libs/compose/*.md"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/compose/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/compose"],
      "options": {
        "jestConfig": "libs/compose/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
