import { stateAdaptorsRecord } from './state-adaptors-record';

describe('stateAdaptorsRecord', () => {
  it('should work', () => {
    expect(stateAdaptorsRecord()).toEqual('state-adaptors-record');
  });
});
