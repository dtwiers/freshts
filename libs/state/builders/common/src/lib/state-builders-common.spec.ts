import { stateBuildersCommon } from './state-builders-common';

describe('stateBuildersCommon', () => {
  it('should work', () => {
    expect(stateBuildersCommon()).toEqual('state-builders-common');
  });
});
